{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/experimental/k-means/k-means"
            },
            "label": "k-means",
            "type": "shell",
            "command": "make -j",
            "problemMatcher": "$gcc"
        }
    ]
}